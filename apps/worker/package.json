{
  "name": "@streamops/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "once": "tsx src/index.ts --once",
    "poison": "tsx src/send-poison-message.ts",
    "dlq:peek": "tsx src/peek-dlq.ts",
    "build:lambda": "esbuild src/lambda.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/lambda.js && node -e \"require('node:fs').writeFileSync('dist/package.json', JSON.stringify({ name: 'streamops-worker-lambda', version: '0.1.0', private: true, main: 'lambda.js', type: 'commonjs' }, null, 2) + '\\n')\"",
    "lambda:check": "tsx -e \"import('./src/lambda.ts').then((m) => console.log(typeof m.handler))\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@streamops/aws": "0.1.0",
    "@streamops/core": "0.1.0"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
